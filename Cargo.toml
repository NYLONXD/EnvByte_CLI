[workspace]
members = ["cli", "server"]
# `cargo run` from the repo root runs the CLI; use `-p greenbyte-server` for the API.
default-members = ["cli"]
resolver = "2"

[profile.release]
opt-level = 3
strip = true
